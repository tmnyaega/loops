# Initialize the password variable as an empty string
password = ""

# Use a while loop that continues until the correct password is entered
while password != "maintain768.@":
    # Prompt the user to input a password and store it as a string in the password variable
    password = str(input("Enter password:"))

# The loop will exit when the entered password matches "maintain768.@"
